import React from "react";

type Props = {};

const NoResult = (props: Props) => {
  return (
    <div className="flex items.enter justify-center h-full w-full text-neutral-500">
      No Result found
    </div>
  );
};

export default NoResult;
